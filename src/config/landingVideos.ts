// Landing-page video assets. All paths are bundled in /public so the demo
// works offline and ships cleanly with the open-source repo — no CDN, no
// expiring URLs.

export const LANDING_VIDEOS = {
    DEMO_PORTRAIT: "/demo-assets/campaign-video.mp4",
};
